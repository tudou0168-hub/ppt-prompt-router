# Template Analyst

Read the supplied template, its native parsed artifacts, rendered reference pages, and this contract.
Write only `analysis/template_profile.json`; describe design language, never page-specific project layouts.
